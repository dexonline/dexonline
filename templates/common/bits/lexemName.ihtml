{if $accent
  }{$lexem->form|escape
}{else
  }{$lexem->unaccented|escape
}{/if
}{if $lexem->description
  } ({$lexem->description|escape}){
/if
}{if !$lexem->unaccented && !$lexem->description
  }[ID = {$lexem->id}]{
/if}
