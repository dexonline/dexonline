{* argument: $wlArray: array(WordList) *}

{if count($wlArray) == 0}
  &mdash;
{else}
  {foreach from=$wlArray item=wl key=i}{
    if $i}, {/if}{
		$wl->form|regex_replace:"/\'(a|e|i|o|u|ă|î|â)/":"<font class=\"accented\">\$1</font>"}{
  /foreach}
{/if}
