{if $smarty.session.flashMessage}
  <div class="flashMessage {$smarty.session.flashMessageType}Type">
    {$smarty.session.flashMessage}
  </div>
  {php}
    unset($_SESSION['flashMessage']);
    unset($_SESSION['flashMessageType']);
  {/php}
{/if}
