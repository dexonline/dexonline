{strip}
  {if $lexem->isLoc && $sUser && ($sUser->moderator & $smarty.const.PRIV_LOC)}
    <span class="isLoc" title="Inclus în lista oficială de cuvinte a jocului de scrabble">[LOC]</span>
  {/if}      
  {if $lexem->source}<br/><span class="lexemTags">Surse flexiune: {$lexem->source}</span>{/if}
  {if $lexem->tags}<br/><span class="lexemTags">{$lexem->tags|escape}</span>{/if}
{/strip}
